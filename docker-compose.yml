version: '3.8'

services:
  ads-data-insight:
    build: .
    ports:
      - "8000:8000"  # API服务
      - "8501:8501"  # Streamlit前端
    volumes:
      - ./data:/app/data
      - ./output:/app/output
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped